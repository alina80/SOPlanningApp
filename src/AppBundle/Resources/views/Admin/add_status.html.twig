{% extends "::admin.html.twig" %}
{% block title %}Add status{% endblock %}
{% block body %}
    <div class="row mt-4">
        <div class="col-6">
            Back to statuses
            <button class="btn btn-outline-primary">
                <a href="{{ path('app_admin_liststatus') }}"><i class="fa fa-arrow-right"></i> </a>
            </button>
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-md-3"></div>
        <div class="col-md-6">
            <legend>Add Status</legend>
            <div class="{% if (errors ?? [])  %}alert-danger{% endif %}">
                {{ (errors ?? []) | join(',') }}
            </div>
            <form action="{{ path('app_admin_savestatus') }}" method="post">
                <div class="form-group">
                    <label for="departmentDescription">Description</label>
                    <input type="text" class="form-control" id="departmentDescription" name="description" placeholder="Enter department">
                </div>

                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
        </div>
        <div class="col-md-3"></div>
    </div>
{% endblock %}
